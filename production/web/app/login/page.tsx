'use client';import{useState}from'react';const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const r=await fetch(`${API}/api/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(r.ok){localStorage.setItem('accessToken',d.accessToken);localStorage.setItem('refreshToken',d.refreshToken);location.href='/'}else setMsg(d.error||'Não foi possível entrar')}return <main className="min-h-screen flex items-center justify-center p-4 bg-night"><form onSubmit={submit} className="w-full max-w-md bg-panel border border-line rounded-2xl p-7"><a href="/" className="text-brand font-black">← Voltar</a><h1 className="text-3xl font-black mt-5">Entrar</h1><input placeholder="E-mail" type="email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full mt-5 bg-night border border-line rounded-lg p-3"/><input placeholder="Senha" type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full mt-3 bg-night border border-line rounded-lg p-3"/>{msg&&<p className="text-red text-sm mt-3">{msg}</p>}<button className="w-full bg-brand text-night font-black py-3 rounded-lg mt-4">Entrar</button><a href="/register" className="block text-center text-sm text-muted mt-4">Ainda não tenho conta</a></form></main>}