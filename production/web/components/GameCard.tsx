import type {CasinoGame} from '../lib/catalog';
export default function GameCard({game,compact=false}:{game:CasinoGame;compact?:boolean}){return <article className="game-card group"><div className={compact?'game-media game-media-compact':'game-media'} style={{backgroundImage:`linear-gradient(180deg,rgba(8,11,15,.02),rgba(8,11,15,.12) 48%,rgba(8,11,15,.94) 100%),url("${game.image}")`}}><div className="game-topline"><span className={game.live?'live-pill':'game-pill'}>{game.live?'● AO VIVO':game.category==='crash'?'CRASH':'SLOT'}</span><span className="provider-pill">{game.provider}</span></div><div className="game-copy"><h3>{game.name}</h3><p>{game.provider}</p><span className="game-action">{game.live?'Abrir mesa':'Abrir jogo'}</span></div></div></article>}
