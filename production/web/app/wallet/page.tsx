'use client';import{useEffect,useState}from'react';import Header from '../../components/Header';import BottomNav from '../../components/BottomNav';import{apiFetch,jsonOrMessage}from'../../lib/api';import{WalletCards,ArrowDownToLine,ArrowUpFromLine,Clock3,Copy,ShieldCheck}from'lucide-react';
export default function Wallet(){const[s,setS]=useState<any>(null),[amount,setAmount]=useState(20),[withdrawAmount,setWithdrawAmount]=useState(50),[pix,setPix]=useState<any>(null),[deposits,setDeposits]=useState<any[]>([]),[withdrawals,setWithdrawals]=useState<any[]>([]),[msg,setMsg]=useState('');async function load(){const[a,b,c]=await Promise.all([apiFetch('/api/wallet'),apiFetch('/api/payments/deposits'),apiFetch('/api/payments/withdrawals')]);if(a.status===401){location.href='/login';return}const ja=await jsonOrMessage(a),jb=await jsonOrMessage(b),jc=await jsonOrMessage(c);if(a.ok)setS(ja.data?.wallet||ja.data);if(b.ok)setDeposits(jb.data);if(c.ok)setWithdrawals(jc.data)}useEffect(()=>{load()},[]);async function deposit(){setMsg('');const r=await apiFetch('/api/payments/pix/deposit',{method:'POST',body:JSON.stringify({amount})});const{data,message}=await jsonOrMessage(r);if(r.ok){setPix(data);setMsg('PIX gerado. O saldo só muda após a confirmação do gateway.');load()}else setMsg(message)}async function withdraw(){setMsg('');const r=await apiFetch('/api/payments/withdrawals',{method:'POST',body:JSON.stringify({amount:withdrawAmount})});const{message}=await jsonOrMessage(r);setMsg(r.ok?'Solicitação de saque criada e enviada para revisão.':message);if(r.ok)load()}function copyPix(){if(pix?.qrCode)navigator.clipboard.writeText(pix.qrCode)}return <main className="min-h-screen pb-20 lg:pb-0"><Header/><div className="app-shell py-5 max-w-6xl"><div className="page-head"><div><div className="eyebrow">Financeiro</div><h1>Carteira</h1><p>Saldo, PIX, saques e histórico conciliados pelo backend.</p></div><WalletCards size={34} className="text-brand hidden sm:block"/></div><section className="grid md:grid-cols-3 gap-4"><div className="panel p-6 md:col-span-1"><span className="text-xs text-muted">Saldo disponível</span><strong className="display text-5xl block mt-1">R$ {Number(s?.balance||0).toFixed(2)}</strong><p className="text-xs text-muted mt-2">Em processamento: R$ {Number(s?.held_balance||0).toFixed(2)}</p><div className="soft-panel p-4 mt-6 flex gap-3"><ShieldCheck size={18} className="text-green shrink-0"/><p className="text-xs text-muted">O sistema não cria saldo fictício. Créditos entram somente por eventos conciliados e idempotentes.</p></div></div><div className="panel p-6"><div className="flex items-center gap-2"><ArrowDownToLine className="text-green"/><h2 className="text-2xl font-bold">Depositar via PIX</h2></div><label className="text-xs text-muted block mt-5">Valor do depósito<input type="number" min={1} value={amount} onChange={e=>setAmount(Number(e.target.value)||20)} className="field"/></label><button onClick={deposit} className="primary-btn w-full mt-4">Gerar PIX</button>{pix&&<div className="soft-panel p-4 mt-4"><div className="text-xs text-muted">Status</div><strong className="text-sm uppercase">{pix.status}</strong>{pix.qrCodeBase64&&<img src={`data:image/png;base64,${pix.qrCodeBase64}`} alt="QR Code PIX" className="w-44 h-44 bg-white p-2 rounded-lg mx-auto mt-4"/>}{pix.qrCode&&<button onClick={copyPix} className="secondary-btn w-full mt-4"><Copy size={14} className="mr-2"/> Copiar PIX copia e cola</button>}</div>}</div><div className="panel p-6"><div className="flex items-center gap-2"><ArrowUpFromLine className="text-accent"/><h2 className="text-2xl font-bold">Solicitar saque</h2></div><label className="text-xs text-muted block mt-5">Valor do saque<input type="number" min={1} value={withdrawAmount} onChange={e=>setWithdrawAmount(Number(e.target.value)||1)} className="field"/></label><button onClick={withdraw} className="secondary-btn w-full mt-4">Enviar para revisão</button><p className="text-xs text-muted mt-3">KYC aprovado é obrigatório. O valor fica reservado enquanto a operação processa o payout.</p></div></section>{msg&&<div className="panel p-4 mt-4 text-sm text-accent">{msg}</div>}<section className="section-gap grid lg:grid-cols-2 gap-4"><History title="Depósitos" icon={<ArrowDownToLine/>} rows={deposits.map(x=>({id:x.id,amount:x.amount,status:x.status,date:x.created_at}))}/><History title="Saques" icon={<ArrowUpFromLine/>} rows={withdrawals.map(x=>({id:x.id,amount:x.amount,status:x.status,date:x.created_at}))}/></section></div><BottomNav/></main>}
function History({title,icon,rows}:{title:string;icon:any;rows:any[]}){return <div className="panel overflow-hidden"><div className="p-5 border-b border-line flex items-center gap-2 text-brand">{icon}<h2 className="text-2xl font-bold text-white">{title}</h2></div><div>{rows.slice(0,8).map(r=><div key={r.id} className="p-4 border-b border-line last:border-0 flex items-center justify-between gap-3"><div><strong>R$ {Number(r.amount).toFixed(2)}</strong><div className="text-xs text-muted mt-1 flex items-center gap-1"><Clock3 size={11}/>{new Date(r.date).toLocaleString('pt-BR')}</div></div><span className="text-xs uppercase text-muted">{String(r.status).replaceAll('_',' ')}</span></div>)}{rows.length===0&&<div className="p-7 text-center text-sm text-muted">Nenhum registro ainda.</div>}</div></div>}
