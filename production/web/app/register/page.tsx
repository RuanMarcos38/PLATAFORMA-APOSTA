'use client';import{useState}from'react';const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000';export default function Register(){const[f,setF]=useState({fullName:'',email:'',phone:'',cpf:'',birthDate:'',password:''}),[msg,setMsg]=useState('');function u(k:string,v:string){setF({...f,[k]:v})}async function submit(e:any){e.preventDefault();const r=await fetch(`${API}/api/auth/register`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...f,cpf:f.cpf.replace(/\D/g,'')})});const d=await r.json();if(r.ok){localStorage.setItem('accessToken',d.accessToken);localStorage.setItem('refreshToken',d.refreshToken);location.href='/wallet'}else setMsg(d.error||'Cadastro não concluído')}return <main className="min-h-screen flex items-center justify-center p-4"><form onSubmit={submit} className="w-full max-w-xl bg-panel border border-line rounded-2xl p-7"><h1 className="text-3xl font-black">Criar conta</h1><p className="text-sm text-muted mt-2">Somente maiores de 18 anos. A liberação financeira depende de KYC.</p><div className="grid sm:grid-cols-2 gap-3 mt-5">{[['fullName','Nome completo','text'],['email','E-mail','email'],['phone','Telefone','tel'],['cpf','CPF','text'],['birthDate','Data de nascimento','date'],['password','Senha','password']].map(([k,l,t])=><input key={k} placeholder={l} type={t} value={(f as any)[k]} onChange={e=>u(k,e.target.value)} className="bg-night border border-line rounded-lg p-3" required/> )}</div>{msg&&<p className="text-red text-sm mt-3">{msg}</p>}<button className="w-full bg-brand text-night font-black py-3 rounded-lg mt-4">Continuar cadastro</button></form></main>}