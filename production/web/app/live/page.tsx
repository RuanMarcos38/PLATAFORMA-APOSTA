import Header from '../../components/Header';import BottomNav from '../../components/BottomNav';import GameCard from '../../components/GameCard';import {liveGames} from '../../lib/catalog';import {Radio,ShieldCheck} from 'lucide-react';
export default function Live(){return <main className="min-h-screen pb-20 lg:pb-0"><Header/><div className="app-shell py-5"><div className="page-head"><div><div className="eyebrow">Cassino ao vivo</div><h1>Mesas e game shows</h1><p>Lobby visual com títulos reais da Evolution e espaço preparado para mesas adicionais do agregador contratado.</p></div><div className="hidden sm:flex items-center gap-2 text-sm text-[#c9d1d9]"><Radio size={17} className="text-red"/> Conteúdo ao vivo</div></div><div className="provider-row mb-5"><span className="provider-chip">Evolution</span><span className="provider-chip">Game Shows</span><span className="provider-chip">Roleta</span><span className="provider-chip">Baccarat</span><span className="provider-chip">Blackjack</span></div><section className="game-grid">{liveGames.map(g=><GameCard key={g.id} game={g}/>)}</section><section className="section-gap panel p-5 flex gap-3 items-start"><ShieldCheck size={20} className="text-green shrink-0"/><div><strong className="text-sm">Sessão do jogo controlada no backend</strong><p className="text-xs text-muted mt-1">A interface exibe o catálogo, mas não incorpora mesa falsa. Quando a licença e o contrato do provedor forem ativados, a rota de launch cria a sessão real e devolve a URL segura do jogo.</p></div></section></div><BottomNav/></main>}
